# Software Messages
diagram:
  id: diag-software-messages
  name: Software Messages
  type: uml:ClassDiagram
  package: pkg-software-messages
  elements:
    - elementId: blk-detectedobjects # DetectedObjects (Block)
    - elementId: blk-predictedobject # PredictedObject (Block)
    - elementId: blk-predictedpath # PredictedPath (Block)
    - elementId: blk-trackedobjects # TrackedObjects (Block)
    - elementId: blk-trajectory # Trajectory (Block)
    - elementId: blk-trajectorypoint # TrajectoryPoint (Block)
    - elementId: cls-crosswalkmodule # CrosswalkModule (Class)
    - elementId: comp-crosswalk-module # Crosswalk Module (Component)
    - elementId: dt-detectedobjects # DetectedObjects (DataType)
    - elementId: dt-predictedobject # PredictedObject (DataType)
    - elementId: dt-predictedobjects # PredictedObjects (DataType)
    - elementId: dt-predictedpath # PredictedPath (DataType)
    - elementId: dt-trackedobjects # TrackedObjects (DataType)
    - elementId: dt-trajectory # Trajectory (DataType)
    - elementId: dt-trajectorypoint # TrajectoryPoint (DataType)
  relationships:
    - trc-cls-crosswalkmodule-to-comp-crosswalk-module # CrosswalkModule → Crosswalk Module (Trace: "trace")
    - abs-dt-detectedobjects-to-blk-detectedobjects # DetectedObjects → DetectedObjects (Abstraction: "realize")
    - abs-dt-predictedobject-to-blk-predictedobject # PredictedObject → PredictedObject (Abstraction: "realize")
    - compos-dt-predictedobject-to-dt-predictedpath # PredictedObject → PredictedPath (Composition)
    - compos-dt-predictedobjects-to-dt-predictedobject # PredictedObjects → PredictedObject (Composition)
    - abs-dt-predictedpath-to-blk-predictedpath # PredictedPath → PredictedPath (Abstraction: "realize")
    - abs-dt-trackedobjects-to-blk-trackedobjects # TrackedObjects → TrackedObjects (Abstraction: "realize")
    - abs-dt-trajectory-to-blk-trajectory # Trajectory → Trajectory (Abstraction: "realize")
    - compos-dt-trajectory-to-dt-trajectorypoint # Trajectory → TrajectoryPoint (Composition)
    - abs-dt-trajectorypoint-to-blk-trajectorypoint # TrajectoryPoint → TrajectoryPoint (Abstraction: "realize")
