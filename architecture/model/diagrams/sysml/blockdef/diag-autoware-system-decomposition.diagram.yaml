# Autoware System Decomposition
diagram:
  id: diag-autoware-system-decomposition
  name: Autoware System Decomposition
  type: sysml:BlockDefinitionDiagram
  package: pkg-system-decomposition
  elements:
    - elementId: blk-ad-api # AD API (Block)
    - elementId: blk-autoware # Autoware System (Block)
    - elementId: blk-camera # Camera (Block)
    - elementId: blk-control # Control (Block)
    - elementId: blk-ego-vehicle # Ego Vehicle (Block)
    - elementId: blk-gnss # GNSS (Block)
    - elementId: blk-imu # IMU (Block)
    - elementId: blk-lidar # LIDAR (Block)
    - elementId: blk-localization # Localization (Block)
    - elementId: blk-map # Map (Block)
    - elementId: blk-perception # Perception (Block)
    - elementId: blk-planning # Planning (Block)
    - elementId: blk-safety-system # Safety System (Block)
    - elementId: blk-sensing # Sensing (Block)
    - elementId: blk-sensor-kit # Sensor Kit (Block)
    - elementId: blk-vehicle-interface # Vehicle Interface (Block)
    - elementId: blk-vlp-16 # VLP-16 (Block)
    - elementId: blk-vls-128 # VLS-128 (Block)
  relationships:
    - compos-blk-autoware-to-blk-ad-api # Autoware System → AD API (Composition)
    - compos-blk-autoware-to-blk-control # Autoware System → Control (Composition)
    - compos-blk-autoware-to-blk-localization # Autoware System → Localization (Composition)
    - compos-blk-autoware-to-blk-map # Autoware System → Map (Composition)
    - compos-blk-autoware-to-blk-perception # Autoware System → Perception (Composition)
    - compos-blk-autoware-to-blk-planning # Autoware System → Planning (Composition)
    - compos-blk-autoware-to-blk-safety-system # Autoware System → Safety System (Composition)
    - compos-blk-autoware-to-blk-sensing # Autoware System → Sensing (Composition)
    - compos-blk-autoware-to-blk-vehicle-interface # Autoware System → Vehicle Interface (Composition)
    - aggr-blk-ego-vehicle-to-blk-sensor-kit # Ego Vehicle → Sensor Kit (Aggregation)
    - compos-blk-sensor-kit-to-blk-camera # Sensor Kit → Camera (Composition)
    - compos-blk-sensor-kit-to-blk-gnss # Sensor Kit → GNSS (Composition)
    - compos-blk-sensor-kit-to-blk-imu # Sensor Kit → IMU (Composition)
    - compos-blk-sensor-kit-to-blk-lidar # Sensor Kit → LIDAR (Composition)
    - gen-blk-vlp-16-to-blk-lidar # VLP-16 → LIDAR (Generalization)
    - gen-blk-vls-128-to-blk-lidar # VLS-128 → LIDAR (Generalization)
